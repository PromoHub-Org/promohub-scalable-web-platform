# PromoHub Backend Environment Configuration
PORT=5000
NODE_ENV=development
JWT_SECRET=promohub_super_secret_jwt_key_2026

# PostgreSQL Connection (Optional: if empty, app automatically uses local SQLite fallback)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/promohub_db

# Frontend CORS Origin
CLIENT_ORIGIN=http://localhost:3000
